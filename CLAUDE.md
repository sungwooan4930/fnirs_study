# CLAUDE.md — 뇌신호 기반 멀티모달 AI 인지상태 추정 · 교수자 증강 XAI 대시보드

## 0. 근거 문서 (최상위 권위)

| 문서 | 경로 |
|------|------|
| **연구계획서 원본 (최상위 근거)** | `docs/plan/[제출본-한상훈]붙임3. 연구과제 신청서 및 연구계획서 등_2차수정본.pdf` |
| 계획서 텍스트 추출본 (grep용) | `docs/plan/연구계획서_텍스트추출.txt` |
| 진행 로그 (갱신 의무) | `run_logging.md` |
| 폐기된 구 프로젝트 문서 | `docs/legacy/PROJECT_GUIDELINES_4ch_superseded.md` |

**이 파일과 연구계획서가 충돌하면 연구계획서가 우선한다.**
계획서에 없는 스펙을 임의로 확정하지 말 것. 모르는 것은 `미확정`으로 남긴다.

---

## 1. 프로젝트 정체성

**이것은 제품 개발이 아니라 1년짜리 연구과제다.** (2026.8 ~ 2027.7, 20,000,000원)

> fNIRS·EEG 뇌신경신호와 학습 수행 데이터를 결합한 멀티모달 AI로 학습자의 **6차원 인지상태**를 실시간 추정하고,
> 그 근거를 교수자가 이해·검증할 수 있는 **설명가능 AI(XAI) 대시보드**로 시각화하며,
> 인지상태에 근거해 **교수전략을 제안**하는 교수자 증강 AI 어시스턴트 **프로토타입**을 개발한다.

핵심 3대 혁신:
1. **크로스-모달 어텐션 융합** — fNIRS(혈류) + EEG(전기) + 행동 로그의 비선형 상호작용 학습
2. **XAI 내재 설계** — 판단 근거를 뇌신호 지표 수준까지 역추적
3. **규칙-데이터 하이브리드 추론** — 교수자를 대체가 아닌 **증강** (수용·수정·거부 구조)

산출물 목표: 논문 3편, 특허 3건, 워킹페이퍼 1편.

### ⚠ 폐기된 이전 방향
`src/`, `web/`의 기존 코드는 **4채널 커스텀 fNIRS 집중도 측정기**로, 본 연구과제와 다른 프로젝트다.
재사용 가능 자산은 mBLL·Butterworth 필터 **로직 참조**뿐이다. 기존 아키텍처·스펙(4ch, 3파장 780/850/950,
BLE, 자체 HDF5 스키마, 1차원 집중도)을 본 과제에 끌어오지 말 것.

---

## 2. 도메인 사전 (코드 명명 규약의 기준)

### 2.1 6차원 인지상태 벡터 — 시스템의 출력
| # | 국문 | 코드 명칭 |
|---|------|-----------|
| 1 | 인지부하 | `cognitive_load` |
| 2 | 주의 | `attention` |
| 3 | 피로 | `fatigue` |
| 4 | 몰입 | `engagement` |
| 5 | 수행정확도 | `accuracy` |
| 6 | 반응지연 | `response_latency` |

### 2.2 4계층 라벨링 체계 — 정답(y)의 출처
| 계층 | 내용 | 코드 명칭 |
|------|------|-----------|
| 1 | 과제 조건 (n-back 수준, 난이도 저/중/고) | `task_condition` |
| 2 | 행동 (정오답률, 반응시간) | `behavior` |
| 3 | 자기보고 (**NASA-TLX**, **KSS**) | `self_report` |
| 4 | 학습성취 | `achievement` |

4계층은 **교차 검증으로 타당도 확보**가 원칙. 단일 계층만으로 라벨을 확정하지 않는다.

### 2.3 측정 모달리티
| 모달 | 스펙 | 후보 지표 |
|------|------|-----------|
| **EEG** | 30채널, 10-5 시스템, 1,000 Hz | θ/α 대역 전력, α/θ 비율, ERP(**P300**, **N200**), 전두 비대칭성 |
| **fNIRS** | 48채널, 전두·두정·운동·후두, 10.4 Hz | PFC·dlPFC **HbO/HbR** 농도 변화, 혈류반응함수(HRF) 특성 |
| **행동** | 자극 제시 로그 | 정오답률, 반응시간 |
| **자기보고** | 과제 후 설문 | NASA-TLX, KSS |

### 2.4 실험 과제
- **n-back**: 0/2/3-back (작업기억 부하 단계 조작)
- **학습 과제**: 텍스트 독해 · 수리 추론 · 개념 학습 (난이도 저/중/고 교차)
- 설계: **피험자 내(within-subject)**, 카운터밸런스로 순서 효과 통제

---

## 3. 파이프라인 규약 (계획서 명시 — 임의 변경 금지)

### 3.1 전처리
```
EEG   : PREP → 필터링 → ASR 아티팩트 보정 → 불량 채널 보간 → 에포크 분할
fNIRS : SCI 프루닝 → 웨이블릿 보정 → 대역통과 필터 → 수정 Beer-Lambert(mBLL) → 에포크 분할
```

### 3.2 시간창
**5초 창 · 1초 스텝** 슬라이딩 윈도우. (→ 80% 오버랩. §5.1 누수 규칙 필독)

### 3.3 융합 3전략 — 반드시 세 가지 모두 구현·비교
| # | 전략 | 구현 |
|---|------|------|
| 1 | 특징 수준 융합 | EEG·fNIRS·행동 특징 결합 → SVM / XGBoost |
| 2 | 결정 수준 융합 | 모달별 분류기 출력 앙상블 |
| 3 | **크로스-모달 어텐션 융합** | CNN(모달별 공간 특징) → LSTM(시간 의존성) → multi-head cross-modal attention |

세 전략의 성능을 **체계적으로 비교**하여 최적 아키텍처를 도출하는 것이 목표 3의 본질이다.
3번만 구현하고 끝내는 것은 계획서 미달이다.

### 3.4 검증 지표
- 3수준 분류 정확도
- **ΔAccuracy** vs 행동 단일 모달 모델 (선행연구 5~15%p 향상 보고)
- **LOSO-CV** (Leave-One-Subject-Out) — 교차 피험자 일반화
- NASA-TLX · 학습성취와의 준거 상관 (r), 계층 회귀 ΔR²
- 계획서 가설 목표치: 이진 **90%**, 다분류 **85%**

### 3.5 XAI
- **SHAP** + **어텐션 가중치** 시각화
- 출력 형식은 특징별 기여도 정량 제시. 계획서 예시:
  > "인지부하 높음" ← 전전두엽 HbO 23% 증가(기여도 0.41), α/θ 비율 저하(0.28), 반응시간 증가(0.18)
- 위험 구간 자동 감지 (임계치 초과 시, **교수자가 임계치 조정 가능**)

### 3.6 교수전략 하이브리드 추론 규칙 (교육학 문헌 기반)
| 인지상태 트리거 | 제안 전략 |
|-----------------|-----------|
| 인지부하 과잉 | 재설명 · 난이도 조정 · 작업 분할 |
| 주의 저하 | 활동 전환 · 질문 |
| 피로 누적 | 휴식 · 과제 전환 |
| 낮은 몰입 | 관련성 부여 · 참여 유도 |

**제안은 반드시 교수자가 `수용 / 수정 / 거부`할 수 있는 구조로 구현한다.** 자동 실행 금지.

### 3.7 표준
- 동기화: **LSL (Lab Streaming Layer)** — 뇌신호·행동·자극 마커 실시간 동기 기록
- 데이터셋: **BIDS** 표준 (재현 가능성 요건)
- 품질관리: fNIRS **SCI**, EEG **임피던스** 기반

---

## 4. 연구 일정 (계획서 §5)

| 개월 | 단계 |
|------|------|
| 1–2 | IRB · 프로토콜 확정 |
| 2–3 | 예비실험 · 보정 |
| 3–5 | 본실험 · 품질관리 |
| 5–8 | AI 모델(크로스-모달 어텐션 융합) · 검증 |
| 8–10 | XAI 대시보드 · 하이브리드 추론 엔진 |
| 11–12 | 평가 · 성과 정리 |

평가: 교수자·전문가 **10~15명**, 혼합 방법(양적 척도 + 질적 인터뷰).
**제어 조건 비교 필수** — 행동 데이터만 대시보드 vs XAI 통합 대시보드의 증분 효과.

---

## 5. 연구 코드 절대 규칙

### 5.1 데이터 누수 방지 — 이 프로젝트 최대 리스크
5초 창·1초 스텝은 **80% 오버랩**이다. 부주의하면 성능이 통째로 허구가 된다.

- **분할 단위는 항상 피험자(subject)다.** 윈도우·에포크 단위 랜덤 분할 **금지**.
- 인접 윈도우가 train/test에 동시 존재하면 그 결과는 폐기한다.
- `StandardScaler`·PCA·특징 선택·클래스 가중치는 **train fold 안에서만 fit**한다. 전체 데이터 fit 금지.
- 하이퍼파라미터 탐색은 **train fold 내부의 중첩(nested) CV**로 한다. test fold를 보고 튜닝 금지.
- 베이스라인 보정·정규화의 기준 구간이 test 구간을 포함하지 않는지 확인한다.

새 평가 코드를 쓰거나 고칠 때는 `.claude/skills/leakage-check` 스킬을 실행한다.

### 5.2 재현성
- 모든 실험은 시드 고정: `numpy`, `random`, `torch`(+`cudnn.deterministic`).
- 전처리·모델 하이퍼파라미터는 **코드가 아니라 `config/*.yaml`**에 둔다.
- 실험 1회 = 결과 디렉토리 1개. config 스냅샷·시드·git commit hash·환경 정보를 함께 저장한다.
- 원시 데이터는 **불변(read-only)**. 어떤 스크립트도 `data/raw/`를 수정하지 않는다.

### 5.3 개인정보 · IRB
- 원시 뇌신호와 참가자 식별정보는 **절대 커밋하지 않는다**.
- 참가자는 `sub-01` 형식의 익명 ID만 사용. 식별정보 매핑표는 리포지토리 밖에 둔다.
- IRB 승인 전 실제 참가자 데이터 수집·처리 코드를 실행하지 않는다.

### 5.4 성능 보고의 정직성
- 정확도를 보고할 때는 **항상 chance level**을 함께 적는다 (3수준 분류 → 33.3%).
- **LOSO-CV 결과와 within-subject 결과를 절대 혼동해 표기하지 않는다.** 반드시 구분 명시.
- 계획서 목표치(이진 90%/다분류 85%)는 **가설이지 달성 사실이 아니다.** 미달을 미달로 보고한다.
- 결과가 나쁘면 나쁘다고 보고한다. 튜닝으로 test 성능을 올린 경우 그 사실을 명시한다.

---

## 6. 개발 프로세스 (필수)

모든 기능 개발은 순서대로 진행한다. 단계를 건너뛰지 않는다.

```
Brainstorm → Plan → TDD → Implement → Debug → Verify → Code Review
```

| 단계 | 스킬 |
|------|------|
| Brainstorm | `superpowers:brainstorming` |
| Plan | `superpowers:writing-plans` |
| TDD | `superpowers:test-driven-development` |
| Implement | `superpowers:executing-plans` |
| Debug | `superpowers:systematic-debugging` |
| Verify | `superpowers:verification-before-completion` |
| Code Review | `superpowers:requesting-code-review` |

독립 하위 작업이 2개 이상이면 `superpowers:dispatching-parallel-agents` 또는
`superpowers:subagent-driven-development`로 병렬 처리한다.

**모든 유의미한 진행은 `run_logging.md`에 기록한다** (`.claude/skills/research-log` 참조).

---

## 7. 기술 스택

| 영역 | 채택 | 비고 |
|------|------|------|
| 언어 | Python 3.11+ | 연구 파이프라인 전체 |
| EEG 처리 | MNE-Python | PREP/ASR은 `pyprep`, `asrpy` |
| fNIRS 처리 | MNE-NIRS | SCI, mBLL 내장 |
| 동기화 | `pylsl` | LSL |
| BIDS | `mne-bids` | + SNIRF(fNIRS) / EDF·BrainVision(EEG) |
| 자극 제시 | PsychoPy | LSL 마커 송출 |
| 고전 ML | scikit-learn, XGBoost | 융합 전략 1·2 |
| 딥러닝 | PyTorch | 융합 전략 3 (CNN-LSTM + cross-modal attention) |
| XAI | SHAP | + 어텐션 가중치 추출 |
| 테스트 | pytest | |
| 대시보드 | **미확정** | §9 참조 |

---

## 8. 디렉토리 구조 (목표)

```
D:/Study_fNIRS/
├── CLAUDE.md                    # 이 파일
├── run_logging.md               # 진행 로그 (갱신 의무)
├── docs/
│   ├── plan/                    # 연구계획서 원본·추출본
│   ├── protocol/                # 실험 프로토콜, IRB 문서
│   ├── specs/                   # 설계 스펙 (superpowers)
│   ├── design/                  # UI·시각화 참고자료 (목표 4)
│   ├── plans/                   # 구현 계획 (superpowers)
│   └── legacy/                  # 폐기된 4ch 프로젝트 문서
├── src/
│   ├── simulation/              # ⭐ 합성 EEG/fNIRS/행동 신호 생성 + LSL 스트림 모킹
│   ├── stimulus/                # PsychoPy 과제 (n-back, 독해·수리·개념)
│   ├── acquisition/             # LSL 수집·동기화·품질관리 (장비 의존 코드는 여기에 격리)
│   ├── preprocessing/
│   │   ├── eeg/                 # PREP→filter→ASR→interp→epoch
│   │   └── fnirs/               # SCI→wavelet→bandpass→mBLL→epoch
│   ├── features/                # EEG·fNIRS·행동 특징 추출
│   ├── datasets/                # BIDS I/O, 윈도잉, 4계층 라벨 결합
│   ├── models/
│   │   ├── feature_fusion/      # 전략 1
│   │   ├── decision_fusion/     # 전략 2
│   │   └── attention_fusion/    # 전략 3
│   ├── evaluation/              # LOSO-CV, 지표, 통계 검정
│   ├── xai/                     # SHAP · 어텐션 기여도
│   ├── strategy/                # 규칙-데이터 하이브리드 추론 엔진
│   └── dashboard/               # 대시보드 백엔드
├── config/                      # YAML 설정 (모든 하이퍼파라미터)
├── data/                        # ⚠ git 제외. BIDS 루트. raw/ 는 read-only
├── results/                     # ⚠ git 제외. 실험 결과·체크포인트
├── tests/
└── legacy_4ch/                  # 폐기된 구 프로젝트 코드 아카이브
```

---

## 9. 확정 / 미확정 사항

### 9.1 확정 (2026-08-18 사용자 결정)
| 항목 | 결정 |
|------|------|
| 개발 착수 지점 | **목표 3 — 전처리 + 모델 파이프라인** 우선 |
| 하드웨어 전략 | **시뮬레이션 우선(Simulation-First)** — 합성 신호 + LSL 스트림 모킹으로 전 파이프라인 개발, 장비 확보 후 드라이버만 교체(HAL 패턴) |
| 구 4ch 코드 | `legacy_4ch/`로 아카이브 완료. 참조 전용, import 금지 |
| 목표 4 대상 (2026-08-19) | 교수자 뷰 **+ 학습자 본인 뷰**. 학습자 프로필 저장, 세션 간 반복측정 변화 표시 |

> ⚠ **학습자 본인 뷰는 계획서 범위 밖의 확장이다.** 계획서 목표 4는 전부 교수자 대상으로
> 서술돼 있다. 사용자 결정으로 채택했으며, 구현 전 두 가지를 반드시 해소해야 한다 —
> **(1) IRB**: 뇌신호 기반 개인 수준 추론을 당사자에게 반환하는 것은 §5.3의 익명화 전제를
> 바꾼다. **(2) 세션 간 비교 가능성**: fNIRS 옵토드 재부착·연습 효과를 처리하지 않으면
> 측정 드리프트를 인지 변화로 표시하게 된다(§5.4 위반).
> 상세: `docs/design/2026-08-19-dashboard-ui-reference.md` §6

### 9.2 미확정 (임의 확정 금지 — 사용자 확인 필요)
1. **참가자 수 — 계획서 내부 불일치. 확인 대기 중.**
   요약·목표2는 `100명`, 추진전략 §3과 예산(30,000원 × 30명)은 `30명`.
   → **코드·문서에 하드코딩 금지.** `config`의 설정값(`n_subjects`)으로만 다루고,
   합성 데이터 생성기는 임의 N을 받도록 설계한다.
2. **실제 보유 장비 모델명** (EEG 30ch / fNIRS 48ch) — LSL 드라이버·SNIRF 몽타주 결정에 필요.
   → 시뮬레이션 단계에서는 장비 중립적 인터페이스로 설계하고 장비 의존 코드를 격리한다.
3. **fNIRS 48채널 몽타주** — 소스-디텍터 배치, SDS(mm), 파장.
   → 합성 데이터에서는 몽타주를 설정 주입 가능하게 만든다.
4. **"3수준 분류"의 정의** — 인지부하 저/중/고인지, 6차원 각각인지.
5. **대시보드 스택** — React+FastAPI(WebSocket) vs Streamlit/Dash. 목표 4 착수 시 결정.
   → 미감 참고자료: `docs/design/2026-08-19-dashboard-ui-reference.md`
   (무채색 Swiss 계열. **미감만 참고. 정보 구조는 1차원 지표 기반이라 §10 위반** — 그대로 옮기지 말 것)
6. `.claude/launch.json`이 폐기된 `web/`·Qt 앱을 참조 중 — 대시보드 스택 확정 후 갱신.

---

## 10. 절대 금지

- ❌ 원시 뇌신호·참가자 식별정보 커밋
- ❌ 피험자 경계를 넘는 데이터 분할 (윈도우 단위 랜덤 split)
- ❌ 전체 데이터에 scaler/특징선택 fit
- ❌ chance level·CV 방식 없이 정확도 보고
- ❌ LOSO 결과와 within-subject 결과 혼용 표기
- ❌ 계획서 미확정 스펙을 임의로 확정
- ❌ 융합 전략 3개 중 일부만 구현하고 "목표 3 완료" 선언
- ❌ 교수전략 제안의 자동 실행 (교수자 수용·수정·거부 구조 필수)
- ❌ 폐기된 4ch 프로젝트 스펙(4채널·3파장·BLE)을 본 과제에 도입
