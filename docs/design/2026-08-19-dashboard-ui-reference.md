# 대시보드 UI 참고자료 — 목업 6종

**날짜:** 2026-08-19
**성격:** **참고자료다. 스펙이 아니다.** 목표 4 착수 시 미감의 출발점으로만 쓴다.
**대상:** 교수자 뷰 + **학습자 본인 뷰** (2026-08-19 사용자 결정, §6)
**관련:** `CLAUDE.md` §9.2-5 (대시보드 스택 미확정), 연구계획서 목표 4

**출처:** 사용자가 저장한 웹페이지에서 추출. 저장본 HTML은 Next.js 클라이언트 렌더링 앱이라
런타임 청크(`/_next/static/chunks/…`)가 캡처되지 않아 **빈 화면으로 렌더링된다.** 확인 후 폐기했고
(2.7 MB → 1.2 MB), 실제 내용인 PNG 6장만 남겼다. `01`은 사용자가 대화에 직접 올린 것과 동일하다.

---

## 1. 목업 카탈로그

| 파일 | 톤 | 정보 구조 | 이 프로젝트에 쓸 것 |
|------|-----|----------|-------------------|
| `01-cognisys-monochrome.png` | 무채색 Swiss, 헤어라인 그리드 | 1차원 `efficiency index 84.2%` + 원형 게이지, `current status: optimal` | **미감만.** 무채색 기반이라 색을 신호 전용으로 남길 수 있다 |
| `02-lab-report-bone-serif.png` | 본(bone) 배경, 세리프, 논문 조판 | **4개 지표를 leader line으로 나열** (RETENTION 0.84 / NEURAL VELOCITY 12.4ms / SIGNAL CLARITY High / **FATIGUE RATIO 0.12**), `FIG. 2A — …/ SESSION 402` 캡션 | **가장 유용.** 다차원 지표 + 그림 캡션 + 랩 리포트 프레이밍 |
| `03-editorial-blue-justified.png` | 청색, 대형 양끝맞춤 본문 | 본문 안에 스탯 박스 인라인 삽입 (`MEAN RETENTION 84.2%`, `NEURAL VARIANCE σ 2.4`), 네비에 **PARTICIPANTS** | 서술 + 수치 병치. σ 표기는 피험자 분산 보고에 참고 |
| `04-cognitive-os-blue-panel.png` | 강한 청색 패널 + 회색 사이드 | 좌: 진행률·세션시계 / 우: **최근 세션 리스트** + `SYSTEM STATUS` | 세션 이력 리스트 패턴 |
| `05-cognitive-os-green-terminal.png` | 형광 녹색, 터미널풍 | 지표 카드 4장 + 커리큘럼 체크리스트 + 원형 `SYNC SCORE 92` | 카드 그리드 배치. 색은 부적합(신호색과 충돌) |
| `06-hurricane-warm-orange.png` | 웜 그레이 + 주황 강조 | **`Subject focus distribution` — 항목별 퍼센트 리스트**, 이상치만 주황 | **기여도 바 차트의 원형.** 이상치만 색을 쓰는 규칙이 유용 |

---

## 2. 채택할 미감

- **무채색/저채도 기반** (01, 02, 06) — 색을 신호 전용으로 남길 수 있다. HbO/HbR 발산 컬러맵
  (청↔백↔적), 위험 구간, 임계 초과 차원이 장식과 경쟁하지 않는다. 연구용 대시보드에서 이건
  미감이 아니라 판독성 문제다.
- **랩 리포트 프레이밍** (02) — `FIG. 2A / SESSION 402` 같은 그림 번호·캡션. 교수자가 근거를
  인용하고 논문에 옮기기 쉬워진다.
- **leader line 지표 목록** (02) — 6차원을 좁은 폭에 나란히 놓기 좋다.
- **이상치만 착색** (06) — 91.0%만 주황. 위험 구간 표시 규칙으로 그대로 쓸 수 있다.
- 헤어라인 보더, 큰 여백, small-caps 라벨, 소문자 대형 헤드라인.

## 3. ⚠ 정보 구조 — 교수자 뷰에는 그대로 쓸 수 없다

여섯 개 전부 **학습자 본인용 자기 모니터링**이다. §6의 결정으로 **학습자 뷰에는 직접 참고**가
되었지만, **교수자용 다인 모니터링 화면에는 아래 격차가 그대로 남는다.** 그리고 1차원 종합
점수와 근거 없는 판정어 문제는 **두 뷰 모두에 해당한다**(§6.4).

| 목업 공통 | 계획서 목표 4 |
|---|---|
| 단일 피험자 (`Subject 402`, `USER_ID 8829-X`) | 다수 학습자 동시 + 집단 분포 |
| 1~4개 지표 | **6차원 벡터** (인지부하·주의·피로·몰입·수행정확도·반응지연) |
| `optimal` / `High` — **판정만** | **판정 근거**: "전전두엽 HbO 23% 증가(기여도 0.41), α/θ 저하(0.28), 반응시간 증가(0.18)" |
| XAI 표면 없음 | **SHAP·어텐션 기여도가 화면의 중심** |
| 없음 | 교수전략 제안 + **수용 / 수정 / 거부** (자동 실행 금지) |
| 없음 | 위험 구간 자동 감지, **교수자가 임계치 조정** |

### 가장 위험한 지점

`01`의 `efficiency index 84.2%`, `05`의 `SYNC SCORE 92` 같은 **단일 종합 점수는 폐기된 4채널
집중도 측정기가 하던 바로 그것**이다. `CLAUDE.md` §10이 명시적으로 금지한다 — 1차원 집중도
지수를 6차원 벡터 대용으로 쓰지 말 것. 레이아웃을 그대로 옮기면 폐기한 자리로 되돌아간다.

`optimal`, `High` 같은 판정어도 같은 계열이다. 목표 4의 핵심은 **AI가 왜 그렇게 판단했는지를
교수자가 이해·검증·수정할 수 있게 하는 것**이므로, 근거 없는 판정어는 그 반대 방향이다.

---

## 4. 미감은 살리고 구조는 바꾸는 매핑

| 목업의 자리 | 대체 |
|---|---|
| `01` 원형 게이지 / `05` SYNC SCORE | 6차원 레이더 또는 6개 스몰멀티플 스파크라인 |
| `02` leader line 4지표 | 그대로 6차원으로 확장 — 이 패턴이 가장 잘 맞는다 |
| `06` `Subject focus distribution` 퍼센트 목록 | **SHAP 기여도 바** (부호 있는 값, 이상치만 착색) |
| `01` `current status: optimal` | 근거 문장 + 트리거된 교수전략 카드 (수용/수정/거부 버튼) |
| `04` 최근 세션 리스트 | 학습자 목록 — 위험 구간 학습자를 상단 고정 |
| `01` 푸터 3분할 (sessions/streak) | 학습자 수 · 위험 학습자 수 · 최근 갱신 시각 |
| `02` `FIG. 2A / SESSION 402` 캡션 | 유지 — 근거 인용 가능성을 높인다 |

---

## 6. 학습자 본인용 뷰 + 종단 프로필 (2026-08-19 사용자 결정)

**결정:** 교수자용 다인 뷰에 더해 **학습자 본인용 단일 피험자 뷰**를 추가한다. 학습자 프로필을
저장해 **반복 측정에 따른 변화**를 표현한다.

이로써 목업 6종의 단일 피험자 구조가 기각이 아니라 **학습자 뷰의 직접적 참고자료**가 된다.
`01`의 원형 게이지·푸터 통계, `04`의 세션 이력 리스트가 특히 그렇다.

### 6.1 계획서와의 관계 — 확장이다

| | 계획서 목표 4 | 이 결정 |
|---|---|---|
| 대상 | **교수자** ("교수자 지원", "교수자 친화적 최소 클릭 UI") | 교수자 **+ 학습자 본인** |
| 시간 범위 | 세션 내 실시간 ("학습자별 6차원 추이"는 교수자 화면 안) | **세션 간 종단** |
| 실험 설계 | 피험자 내, 카운터밸런스, 세션 1회 | 반복 측정 |

계획서에 없는 확장이므로 **임의 확정이 아니라 사용자 결정으로 기록**한다. 1년·2천만원 범위와
산출물(논문 3편·특허 3건)이 교수자 증강으로 프레이밍돼 있다는 점은 별도로 유효하다.

### 6.2 ⚠ IRB — 구현 전 반드시 해소

`CLAUDE.md` §5.3은 `sub-01` 익명 ID만 쓰고 식별정보 매핑표를 리포지토리 밖에 두라고 규정한다.
학습자 본인 뷰는 이 전제를 바꾼다:

- **신원 연결이 필요하다** — 누가 자기 데이터를 보는지 인증해야 한다
- **개인별 인지상태 추정치를 지속 저장한다** — 세션 간 비교의 전제
- **뇌신호 기반 개인 수준 추론을 당사자에게 반환한다**

마지막 항목이 질적으로 다르다. "당신의 피로도가 세션에 걸쳐 상승했습니다"는 집단 연구 데이터
활용과 다른 동의 사안이다. 통상 IRB는 개인 수준 결과 반환을 별도로 다루며, **표시된 추정이
틀렸을 때 / 당사자가 불안을 느낄 때의 대응 계획**을 요구한다. 모델 정확도가 다분류 85% 가설
수준임을 감안하면 더욱 그렇다.

→ **구현 착수 전 IRB 프로토콜에 반영해야 한다.** 계획서 일정상 IRB는 1~2개월차이므로 늦지 않다.

### 6.3 ⚠ 종단 비교의 측정 타당도

세션 간 변화를 보이려면 세션 간 비교 가능성을 먼저 확보해야 한다. 현재 계획서 설계에는 없다.

| 위험 | 내용 |
|---|---|
| **fNIRS 옵토드 재부착** | 세션마다 위치가 달라져 베이스라인이 이동한다. 인지 변화처럼 보인다 |
| **EEG 임피던스·캡 위치** | 동일 |
| **연습 효과** | n-back은 반복하면 쉬워진다. 인지부하 저하가 학습인지 적응인지 분리 필요 |
| **일간 변동** | 수면·카페인·시간대. 계획서의 KSS는 세션 내 측정이다 |

→ 세션마다 **공통 베이스라인 블록**을 두고 개인 내 정규화를 하거나, 절대값이 아닌
**동일 조건 대비 상대 변화**만 표시해야 한다. 처리하지 않으면 측정 드리프트를 인지 변화로
표시하게 된다 — `CLAUDE.md` §5.4의 정직성 규정에 걸린다.

### 6.4 1차원 지표 함정이 여기서 다시 온다

"시간에 따른 변화"는 단일 점수를 쓰고 싶어지는 자리다. `01`의 `efficiency index 84.2%`가 정확히
그 형태다. 학습자 뷰에서도 **6차원을 유지**한다. 종합 점수를 쓰려면 그 정의와 가중치를 명시하고
근거를 함께 표시해야 하며, 그렇지 않으면 폐기한 4채널 집중도 측정기로 되돌아간다
(`CLAUDE.md` §10).

### 6.5 학습자 뷰에 매핑

| 목업 | 학습자 뷰에서의 쓸모 |
|---|---|
| `01` 푸터 3분할 (sessions / total time / streak) | 세션 수·누적 측정시간은 그대로 유효 |
| `01` 원형 게이지 | **6차원 레이더로 교체.** 이번 세션 vs 개인 기준선 오버레이 |
| `04` 최근 세션 리스트 | **그대로** — 세션 이력 진입점 |
| `02` leader line 지표 | 6차원 각각의 세션 간 변화량(Δ)을 나란히 |
| `06` 이상치만 착색 | 유의하게 변한 차원만 강조 |
| `03` `σ 2.4` | 개인 내 변동성 표기 참고 |

### 6.6 아직 결정하지 않은 것

- 학습자에게 **어느 차원까지** 보일지 (피로·인지부하는 민감할 수 있다)
- 추정 불확실성을 어떻게 표시할지 — 점추정만 보이면 과신을 부른다
- 프로필 저장 위치·보존 기간·삭제 요청 처리 (개인정보 처리방침 사안)
- 교수자 뷰와 학습자 뷰가 **같은 앱인지 분리된 앱인지**

---

## 7. 아직 결정하지 않은 것 (공통)

- **대시보드 스택** (`CLAUDE.md` §9.2-5): React+FastAPI(WebSocket) vs Streamlit/Dash. 미확정.
- 실시간성 요구 수준 — 계획서는 "실시간 시각화"라고만 명시. 갱신 주기 미정.
- 한 화면에 학습자 몇 명까지 표시할지.
- 6차원을 한 화면에 모두 보일지, 우선 차원만 보이고 나머지는 드릴다운할지.

이 문서는 위 결정들을 **하지 않는다.** 목표 4 착수 시 `superpowers:brainstorming`으로 연다.
